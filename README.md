# GH2GDrive

Upload files directly to **Google Drive** with GitHub Actions.

Ready-to-use workflows for downloading files from Direct URLs, YouTube, Telegram, and more, then automatically uploading them to Google Drive.

## Features

* 📥 Direct URL → Google Drive
* 📺 YouTube Video/Audio → Google Drive (no YouTube cookies are needed)
* 💬 Telegram → Google Drive
* ⚡ FAST workflow variants with dependency caching
* 📁 Supports Google Drive Folder IDs, URLs, and paths
* 🚀 Runs entirely on GitHub Actions (no VPS required)

---

# Usage (One-Time Setup)

## Google Cloud Setup

> **Note:** It is recommended to perform the Google Cloud setup using a clean Google account and a US IP address.

### 1. Create a Google Cloud Project

1. Go to https://console.cloud.google.com
2. Sign in.
3. Create a new project (the project name can be anything).

### 2. Enable Google Drive API

1. Open **APIs & Services → Enabled APIs & Services**.
2. Make sure your newly created project is selected.
3. Click **Enable APIs and Services**.
4. Search for **Google Drive API** and enable it.

### 3. Configure OAuth Consent Screen

1. Open **APIs & Services → Credentials**.
2. Click **Create Credentials → OAuth Client ID**.
3. If prompted, click **Configure Consent Screen**.
4. Click **Get Started**.
5. Enter any App Name.
6. Use your Google account as the Support Email.
7. Select **External** as the Audience.
8. Enter a Contact Email.
9. Accept the Google API User Data Policy.
10. Finish creating the consent screen.

### 4. Create OAuth Credentials

1. Click **Credentials → Create OAuth Client ID**.
3. Select **Desktop App** for Application type.
4. Enter any name and Click **Create**.
4. Download the OAuth credentials JSON and rename it to **`client_secret.json`**.

### 5. Add Test Users

1. Open the **Audience** section of the OAuth Consent Screen.
2. Under **Test Users**, click **Add Users**.
3. Add the Gmail addresses that should be allowed to authorize the application.

> Only Gmail accounts listed here can generate refresh tokens and access their own Google Drive through this application.

# Generate Google Drive Tokens

This repository includes **`token_gen.py`**, which automatically generates all required GitHub Secrets.

## Requirements

* Python 3
* `client_secret.json` downloaded from Google Cloud

Place **`client_secret.json`** and **`token_gen.py`** in the same directory, then run:

```bash
python token_gen.py
```

Your browser will open.

1. Sign in using one of the **Test Users** you added earlier.
2. Click **Continue**.
3. Approve the requested Google Drive permissions.

The script will print three values similar to:

```text
GDRIVE_CLIENT_ID = xxxxxxxxxxxxxxxxxxxxxxxx.apps.googleusercontent.com

GDRIVE_CLIENT_SECRET = XXXXXXXXXXXXXXXXXXXXXXXX

GDRIVE_REFRESH_TOKEN = 1//XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
```

Save these values for GitHub.

# Configure GitHub

Fork this repository, then open **Settings → Secrets and variables → Actions** and create these repository secrets:

| Secret               | Value                     |
| -------------------- | ------------------------- |
| GDRIVE_CLIENT_ID     | Generated by token_gen.py |
| GDRIVE_CLIENT_SECRET | Generated by token_gen.py |
| GDRIVE_REFRESH_TOKEN | Generated by token_gen.py |

Next, open **Settings → Actions → General** and configure:

### Actions permissions

```
Allow all actions and reusable workflows
```

### Workflow permissions

```
Read and write permissions
```

Save the changes.

---

# Running a Workflow

Open the **Actions** tab, select a workflow, click **Run workflow**, fill in the required inputs, and start it.

The uploaded files will appear in your Google Drive when the workflow completes.


# Workflow Variants

Each workflow has two versions.

## Standard

Installs all dependencies from scratch on every run.

Typical startup time: **60–75 seconds**

## FAST

Caches the Python virtual environment and dependencies.

- First run: about the same as Standard.
- Later runs: typically **15–30 seconds faster**.

If you run workflows frequently, the FAST variants are recommended.

# Google Drive Folder Input

Workflows accept any of these formats.

**Folder ID**

```text
1AbCdEfGhIjKlMnOpQrStUvWxYz
```

**Folder URL**

```text
https://drive.google.com/drive/folders/1AbCdEfGhIjKlMnOpQrStUvWxYz
```

**Folder Path**

```text
Movies
Downloads/Anime
Telegram/Movies
```

---

# Telethon

Telethon is optional. It downloads Telegram posts and media and generally performs better than `yt-dlp`.

If `TG_SESSION`, `TG_API_ID` and `TG_API_HASH` are present as Github secrets, Telethon will be used; otherwise the workflow falls back to `yt-dlp`.

## Setting Up Telethon

### 1. Create a Telegram API Application

1. Log in to **https://my.telegram.org**.
2. Click **API development tools**.
3. Fill out the form:
   - **App title:** Any name
   - **Short name:** Any unique name
   - **Platform:** Desktop (or any platform)
4. Click **Create application**.
5. Save **API ID** as `TG_API_ID` and **API Hash** as `TG_API_HASH`.

> Alternatively, You can use Telegram Desktop's official API
> ```
> TG_API_ID = 2040
> TG_API_HASH = b18441a1ff607e10a989891a5462e627
> ```

### 2. Generate a Session

Install Telethon:

```bash
pip install telethon
```

Create `generate_session.py`, make sure to add your `API_ID` and `API_HASH` values :

```python
from telethon.sync import TelegramClient
from telethon.sessions import StringSession

API_ID = 12345678                # Replace with your API ID
API_HASH = "your_api_hash"       # Replace with your API Hash

with TelegramClient(StringSession(), API_ID, API_HASH) as client:
    print(client.session.save())
```

Run:

```bash
python generate_session.py
```

The first time, Telethon asks for your phone number, login code, and 2FA password (if enabled), then prints a string like:

```text
1AQAOMT...
```

Copy it as your `TG_SESSION`.

### 3. Add GitHub Secrets

Open **Settings → Secrets and variables → Actions → New repository secret** and create:

| Secret Name   | Value                        |
|---------------|------------------------------|
| `TG_API_ID`   | Your API ID                  |
| `TG_API_HASH` | Your API Hash                |
| `TG_SESSION`  | The generated session string |

## Security Notes

- Never commit `TG_SESSION`, `GDRIVE_REFRESH_TOKEN`, `GDRIVE_CLIENT_SECRET`, or `GDRIVE_CLIENT_ID`.
- Never share `TG_SESSION`; it grants access to your Telegram account.
- If leaked, revoke it from **Telegram → Settings → Devices** and generate a new one.
- `GDRIVE_REFRESH_TOKEN` can obtain new access tokens without logging in again.
- Anyone with the refresh token, client ID, and client secret may be able to access your Google Drive according to the granted OAuth permissions.
- If compromised, revoke the application's access (or OAuth credentials) and generate a new refresh token.

# License

This project is provided as-is. Feel free to modify and use it in your own projects.
